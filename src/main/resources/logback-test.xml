<configuration debug="true" scan="true" scanperiod="2 seconds">

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{yyyy-MM-dd}-%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>log/order_logs/Order_%d{yyyy-MM-dd}_%d{HH:mm:ss.SSS}.log</fileNamePattern>

            <!-- Keep 7 days' worth of history -->
            <maxHistory>3</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>[%d{HH:mm:ss.SSS}] %-4relative [%thread] %-5level %logger{36} [%file:%line] - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.paypal.base" level="DEBUG">
        <appender-ref ref="FILE" />
    </logger>


    <logger name="com.codecool.shop.controller" level="ALL" additivity="false">
        <appender-ref ref="FILE" />
    </logger>

    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>